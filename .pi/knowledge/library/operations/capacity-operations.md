---
type: concept
title: 容量规划：验证与运维视角
description: 让 Agent 服务在启动、调用、流式输出、错误和容量变化时都可诊断，形成从日志到指标、追踪和运行手册的闭环。估算文件数、正文大小、并发、内存和模型成本的增长曲线，本文从验证与运维视角展开。
resource: .pi/knowledge/library/operations/capacity-operations.md
tags: [Pi, Agent, 知识库, operations, capacity, 运行与可观测性]
status: active
verified: true
updated: 2026-08-01
generated_by: luna
generator_model: luna
generator_reasoning: low
generator_mode: local-subprocess
domain: operations
topic: capacity
variant: operations
---

# 容量规划：验证与运维视角

## 摘要

本文服务于需要观察性能、稳定性和故障恢复的工程师。它讨论的不是一个孤立的 API，而是“估算文件数、正文大小、并发、内存和模型成本的增长曲线”在一个可观察 Agent 系统中的完整边界：什么由用户提供，什么由模型判断，什么必须由程序验证，什么只能作为未知返回。文章采用文件优先的知识方式，所有结论都应该能够回到本文件的结构、示例、指标和验证问题；如果读者需要更细的实现细节，应沿着文末的相关知识继续阅读，而不是把本篇当成没有版本的永久真理。

本篇的使用方式是先读摘要和问题边界，再根据任务跳到核心模型、实施流程或失败模式。对于线上问答，检索器可以使用标题、标签和正文召回本篇，Agent 仍然需要判断返回内容是否真的回答了用户问题。对于性能测试，本篇的长度、段落结构、重复术语和交叉链接会模拟真实知识库中的长文差异，便于比较文件扫描、索引和模型上下文的成本。

## 问题边界

“容量规划”属于“运行与可观测性”这个主题域。主题域的目标是让 Agent 服务在启动、调用、流式输出、错误和容量变化时都可诊断，形成从日志到指标、追踪和运行手册的闭环。本篇聚焦“估算文件数、正文大小、并发、内存和模型成本的增长曲线”，不负责替代相邻主题的完整规范，也不把一个示例提升为所有项目都必须采用的规则。把成功、失败、延迟、容量和恢复证据都记录下来，避免只看一次成功请求。

需要特别区分三类信息。第一类是可直接检查的事实，例如文件路径、字段、事件、数据库记录或测试输出；第二类是结合事实得出的推论，例如某个延迟可能来自多次工具往返；第三类是还缺少证据的未知，例如模型是否在另一种 provider 上产生同样的 thinking 事件。问答时如果三类信息混在一起，文章即使很长也会制造错误确定性。

这篇文章还把故障检测和用户体验作为第一层边界，把部署一致性和回滚作为第二层边界。前者回答“系统允许观察或操作什么”，后者回答“发生变化时谁负责验证”。只有当两层边界都写清楚，容量规划才适合进入 Agent 的检索上下文；否则它更像一段容易被误读的意见。

## 核心模型

1. **故障检测和用户体验**：围绕“估算文件数、正文大小、并发、内存和模型成本的增长曲线”定义一个可检查的边界。落地时要产生 `metrics dashboard`，并说明它来自哪一层事实、由谁维护、在什么条件下失效。
2. **部署一致性和回滚**：围绕“估算文件数、正文大小、并发、内存和模型成本的增长曲线”定义一个可检查的边界。落地时要产生 `trace span map`，并说明它来自哪一层事实、由谁维护、在什么条件下失效。
3. **指标阈值和行动**：围绕“估算文件数、正文大小、并发、内存和模型成本的增长曲线”定义一个可检查的边界。落地时要产生 `health contract`，并说明它来自哪一层事实、由谁维护、在什么条件下失效。
4. **容量趋势和成本**：围绕“估算文件数、正文大小、并发、内存和模型成本的增长曲线”定义一个可检查的边界。落地时要产生 `incident runbook`，并说明它来自哪一层事实、由谁维护、在什么条件下失效。
5. **可观测性字段和隐私**：围绕“估算文件数、正文大小、并发、内存和模型成本的增长曲线”定义一个可检查的边界。落地时要产生 `request log schema`，并说明它来自哪一层事实、由谁维护、在什么条件下失效。
6. **故障检测和用户体验**：围绕“估算文件数、正文大小、并发、内存和模型成本的增长曲线”定义一个可检查的边界。落地时要产生 `metrics dashboard`，并说明它来自哪一层事实、由谁维护、在什么条件下失效。
7. **部署一致性和回滚**：围绕“估算文件数、正文大小、并发、内存和模型成本的增长曲线”定义一个可检查的边界。落地时要产生 `trace span map`，并说明它来自哪一层事实、由谁维护、在什么条件下失效。

以上模型可以压缩成一条可追踪链路：问题进入后先确定范围，再取得事实，之后由 Agent 或应用选择下一步，最后生成带证据的回答。任何一步缺失，都应该在事件或回答中留下缺口，而不是用一句“系统已处理”掩盖。对于验证与运维视角，最值得保留的是“问题—证据—决策—结果—验证”这五个连接点。

## 设计决策

### 决策 1：把指标阈值和行动变成可验证选择
不能只写“支持”或“性能好”。应该先写出适用范围，再列出一个反例和一个退出条件；例如在需要观察性能、稳定性和故障恢复的工程师看来，指标阈值和行动必须能被 模型首 token p95 观察到，超过预算时要回到上一级方案重新判断。

### 决策 2：把容量趋势和成本变成可验证选择
不能只写“支持”或“性能好”。应该先写出适用范围，再列出一个反例和一个退出条件；例如在需要观察性能、稳定性和故障恢复的工程师看来，容量趋势和成本必须能被 内存峰值 观察到，超过预算时要回到上一级方案重新判断。

### 决策 3：把可观测性字段和隐私变成可验证选择
不能只写“支持”或“性能好”。应该先写出适用范围，再列出一个反例和一个退出条件；例如在需要观察性能、稳定性和故障恢复的工程师看来，可观测性字段和隐私必须能被 知识加载耗时 观察到，超过预算时要回到上一级方案重新判断。

### 决策 4：把故障检测和用户体验变成可验证选择
不能只写“支持”或“性能好”。应该先写出适用范围，再列出一个反例和一个退出条件；例如在需要观察性能、稳定性和故障恢复的工程师看来，故障检测和用户体验必须能被 请求成功率 观察到，超过预算时要回到上一级方案重新判断。

### 决策 5：把部署一致性和回滚变成可验证选择
不能只写“支持”或“性能好”。应该先写出适用范围，再列出一个反例和一个退出条件；例如在需要观察性能、稳定性和故障恢复的工程师看来，部署一致性和回滚必须能被 工具错误率 观察到，超过预算时要回到上一级方案重新判断。

### 决策 6：把指标阈值和行动变成可验证选择
不能只写“支持”或“性能好”。应该先写出适用范围，再列出一个反例和一个退出条件；例如在需要观察性能、稳定性和故障恢复的工程师看来，指标阈值和行动必须能被 模型首 token p95 观察到，超过预算时要回到上一级方案重新判断。

这些决策的共同原则是可替换。知识源可以从 Markdown 换成 SQLite FTS5 或其他索引，Web 可以从 SSE 换成别的传输，模型也可以更换 provider，但 metrics dashboard、trace span map 和 health contract 中记录的事实不能随着技术替换而消失。这样做的价值不是提前猜中未来，而是给未来留下一个可以比较的基线。

## 实施流程

1. **识别问题**：把用户问题改写成一个可以被证据回答的任务，但不要改变原始意图。
2. **确认范围**：确认本篇内容只覆盖“容量规划”，并把相邻主题 结构化日志、运行指标、分布式追踪 作为可能的后续引用。
3. **准备事实**：加载或查询 metrics dashboard，检查状态、版本、更新时间和必要的权限条件。
4. **执行主流程**：按照 部署一致性和回滚 的顺序执行，不把模型猜测、用户输入和工具返回混在同一个不可审计的字符串中。
5. **记录事件**：在关键节点记录 health contract，至少能还原开始、成功、失败、重试和取消五种状态。
6. **处理异常**：遇到 健康检查只看端口 时先停止扩大影响，返回已知证据、缺失信息和下一步，而不是为了完整而补写未知事实。
7. **验证结果**：用 内存峰值 和一条人工可读的检查结论确认结果，必要时让另一个人复核边界案例。
8. **发布维护**：将文章、索引、测试样例和变更记录一起发布，下一次 可观测性字段和隐私 变化时重新运行本篇的验证。

在实现时不要把所有步骤塞进一个超长函数。可以让边界模块负责解析和验证，让检索或数据模块负责返回证据，让 Agent 适配层负责 session 与事件，让 API 层负责身份、流式传输和取消。这样每个模块都能用独立夹具测试，发生失败时也能知道是事实缺失、能力错误、模型行为还是传输问题。

## 可运行示例

下面的配置不是生产权限声明，而是一个用于验证本主题的最小场景描述。它把主题、证据、指标和负责人放在一起，便于测试脚本或 Agent 在回答后检查是否遗漏关键信息。

```yaml
scenario: operations-capacity-operations
question: "在压力、断连、旧数据或依赖失败时，系统应留下什么可诊断证据？"
evidence:
  source: ".pi/knowledge/library/operations/capacity-operations.md"
  status: active
  required_fields:
    - metrics dashboard
    - trace span map
verification:
  metric: 工具错误率
  owner: knowledge-benchmark
```

在这个场景中，输入应当包含一个明确问题和必要范围；处理过程应当能找到至少一个真实文件或数据证据；输出应当带有来源、状态和验证指标。如果只返回一段看似流畅的文字，却不能解释为什么选择这个证据，测试应该判定为“语言完成但证据不完整”。反过来，如果证据不足，合格的回答可以短一些，但必须明确告诉用户缺了什么。

## 性能与质量验证

- **工具错误率**：先建立当前基线，再观察 验证与运维视角 变更后的差异。若出现“告警不可行动”，应把这次结果标为不可比较，补齐输入、版本或环境信息后再测。
- **模型首 token p95**：先建立当前基线，再观察 验证与运维视角 变更后的差异。若出现“回滚漏掉知识快照”，应把这次结果标为不可比较，补齐输入、版本或环境信息后再测。
- **内存峰值**：先建立当前基线，再观察 验证与运维视角 变更后的差异。若出现“容量增长没有基线”，应把这次结果标为不可比较，补齐输入、版本或环境信息后再测。
- **知识加载耗时**：先建立当前基线，再观察 验证与运维视角 变更后的差异。若出现“日志没有 session 关联”，应把这次结果标为不可比较，补齐输入、版本或环境信息后再测。
- **请求成功率**：先建立当前基线，再观察 验证与运维视角 变更后的差异。若出现“健康检查只看端口”，应把这次结果标为不可比较，补齐输入、版本或环境信息后再测。
- **工具错误率**：先建立当前基线，再观察 验证与运维视角 变更后的差异。若出现“告警不可行动”，应把这次结果标为不可比较，补齐输入、版本或环境信息后再测。

性能不能只看单次命令的平均耗时。文件数增加后，目录遍历、打开文件、解析 frontmatter、正文扫描和结果序列化可能分别成为瓶颈；模型调用则还会叠加 thinking、工具往返、上下文长度和生成时间。建议至少记录冷启动、热缓存、空结果、单命中、多命中、长文和并发六种条件，并把本篇的资源路径、文章长度、检索 query 和版本写进基准报告。

质量也不能只看命中数量。真正有用的结果需要同时满足相关性、覆盖关键条件、状态有效、引用可打开和不会把文档里的普通文本当成执行指令。对于“容量规划”，如果 Top-K 中有很多相邻主题但没有本篇，应该检查标题与标签权重；如果召回本篇却答错，应该检查片段切分、证据阅读和最终回答的 groundedness。

## 失败模式与恢复

### 失败模式 1：健康检查只看端口
常见原因不是单一代码错误，而是边界没有落到 incident runbook。排查时先保留原始请求和实际事件，再确认哪一个条件未满足；如果无法确认，就输出未知并进入人工或下一步诊断。

### 失败模式 2：告警不可行动
常见原因不是单一代码错误，而是边界没有落到 request log schema。排查时先保留原始请求和实际事件，再确认哪一个条件未满足；如果无法确认，就输出未知并进入人工或下一步诊断。

### 失败模式 3：回滚漏掉知识快照
常见原因不是单一代码错误，而是边界没有落到 metrics dashboard。排查时先保留原始请求和实际事件，再确认哪一个条件未满足；如果无法确认，就输出未知并进入人工或下一步诊断。

### 失败模式 4：容量增长没有基线
常见原因不是单一代码错误，而是边界没有落到 trace span map。排查时先保留原始请求和实际事件，再确认哪一个条件未满足；如果无法确认，就输出未知并进入人工或下一步诊断。

### 失败模式 5：日志没有 session 关联
常见原因不是单一代码错误，而是边界没有落到 health contract。排查时先保留原始请求和实际事件，再确认哪一个条件未满足；如果无法确认，就输出未知并进入人工或下一步诊断。

### 失败模式 6：健康检查只看端口
常见原因不是单一代码错误，而是边界没有落到 incident runbook。排查时先保留原始请求和实际事件，再确认哪一个条件未满足；如果无法确认，就输出未知并进入人工或下一步诊断。

恢复流程必须留下可复盘记录：原始问题、检索 query、候选来源、实际读取的文件、工具事件、模型配置、最终答案和人工修正。只有这样，下一次改写文章或索引时才能判断是知识缺口、召回缺口、模型解释错误还是前端展示遗漏。恢复不是把所有错误都重试一次；当输入没有权限或知识确实不存在时，重试只会增加延迟和误导。

## 问答测试样例

1. 用户问“容量规划最重要的边界是什么？”时，答案是否同时引用了 `metrics dashboard` 和 `trace span map`？
   预期：回答应回到本文证据，并在缺少条件时明确说明不能直接下结论。
2. 用户把“估算文件数、正文大小、并发、内存和模型成本的增长曲线”与 结构化日志 混在一起时，Agent 能否先澄清范围？
   预期：回答应回到本文证据，并在缺少条件时明确说明不能直接下结论。
3. 当 健康检查只看端口 发生时，回答是否明确区分已知事实、推论和未知？
   预期：回答应回到本文证据，并在缺少条件时明确说明不能直接下结论。
4. 如果把 工具错误率 提高一倍，系统是否会改变上下文、成本或安全预算？
   预期：回答应回到本文证据，并在缺少条件时明确说明不能直接下结论。
5. 一次成功调用能否从 health contract 还原，而不是只剩最终自然语言？
   预期：回答应回到本文证据，并在缺少条件时明确说明不能直接下结论。
6. 这篇文章中的例子是否给出了输入、处理、输出和验证证据，而不是只有概念描述？
   预期：回答应回到本文证据，并在缺少条件时明确说明不能直接下结论。
7. 内容更新后，旧的引用、链接和索引是否仍能解析到同一个稳定主题？
   预期：回答应回到本文证据，并在缺少条件时明确说明不能直接下结论。
8. 在压力、断连、旧数据或依赖失败时，系统应留下什么可诊断证据？
   预期：回答应回到本文证据，并在缺少条件时明确说明不能直接下结论。

执行这些问题时，测试记录应保存 query、Top-K 文件、首个相关结果的排名、返回字符数、耗时和最终答案是否引用实际来源。对于没有答案的问题，还应加入一条负向期望：Agent 不得从相似文章拼接出不存在的事实。这样，长文档不仅是模型上下文，也是可量化的测试夹具。

## 维护与变更

文章发布后，至少要维护标题、资源路径、状态、更新时间、标签和相关链接。事实变化时先更新正文，再更新验证问题与示例中的指标；如果只有措辞变化，也应在变更记录中说明是否影响检索词和答案依据。建议通过自动校验检查每篇文章的 frontmatter、最小长度、重复标题和悬空链接，并在基准测试中保留本次数据集的文件数与总字符数。

验证与运维视角最容易忽略的是维护成本。长文不是越长越好，新增内容必须带来新的边界、例外、证据或验证价值；如果一段文字不能帮助读者做决策、定位故障或判断答案，它就应该被删除或移动到更合适的主题。保持这种纪律，才能让四五百篇知识真正提高问答覆盖，而不是只增加扫描和上下文负担。

## 相关知识

- [结构化日志](/.pi/knowledge/operations/logging-architecture.md)：从同一主题域的相邻角度补充“记录 request、session、turn 和 tool 维度，避免只打印自然语言”。
- [运行指标](/.pi/knowledge/operations/metrics-architecture.md)：从同一主题域的相邻角度补充“用计数器、直方图和 gauges 描述流量、延迟、错误和资源”。
- [分布式追踪](/.pi/knowledge/operations/tracing-architecture.md)：从同一主题域的相邻角度补充“连接浏览器请求、API、检索、模型和工具执行的时间线”。

## 结论

围绕“容量规划”的可靠回答，最终要回到三个问题：证据在哪里，决策由谁做，结果如何验证。本文提供的是一份可检索、可引用、可压测的长文样本；真实系统仍应以当前代码、版本化知识源和运行时观测结果为准。任何无法由这些证据支持的结论，都应该被标为推论或未知，而不是因为文章篇幅足够长就获得额外的可信度。
