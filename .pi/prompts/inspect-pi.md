---
description: 检查当前 Pi 项目的资源、工具边界、Session 和运行指标
argument-hint: "[检查重点]"
---

请检查当前 Pi Workbench 项目的 ${1:-资源加载、工具权限、Session 持久化和运行指标}。

要求：

1. 先说明检查范围和已知边界。
2. 需要项目事实时，调用只读 `search_knowledge`，必要时调用 `read`。
3. 分开列出 Pi 官方能力、当前项目实现和缺失能力。
4. 引用实际使用的 `.pi` 文件路径，并给出可验证的下一步。
