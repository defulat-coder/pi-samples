{
  "name": "@pi-workbench/pi-agent",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "dependencies": {
    "@earendil-works/pi-coding-agent": "^0.84.2",
    "@earendil-works/pi-tui": "0.84.2",
    "@pi-workbench/contracts": "workspace:*",
    "better-sqlite3": "^13.0.3",
    "pi-permission-system": "0.8.0"
  },
  "scripts": {
    "build": "node scripts/build-permission-ext.mjs && tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "node --test dist/**/*.test.js",
    "lint": "tsc -p tsconfig.json --noEmit && eslint --config ../../eslint.config.js src",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "@earendil-works/pi-ai": "0.84.2",
    "@pi-workbench/eslint": "workspace:*",
    "@types/better-sqlite3": "^9.6.0",
    "esbuild": "^0.28.2",
    "eslint": "^10.9.0",
    "tsx": "^4.23.12"
  }
}
